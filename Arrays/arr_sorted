def check(arr):
    for i in range(len(arr)-1):
        if arr[i]>arr[i+1]:
            return False
    return True


arr=[1,2,4,6,8]
if check(arr):
    print("sorted")
else:
    print("Not Sorted")
